{
  "eval_id": 3,
  "eval_name": "api-500-nearby",
  "prompt": "GET /api/trails/nearby keeps returning 500. The map on the home page won't load — it just shows a spinner. I haven't changed anything in the API recently.",
  "assertions": [
    "The agent checked gaku-api pod logs (or attempted to curl the endpoint) to find the exception",
    "The agent traced through the code stack: endpoint → service → repository (not just 'there's an error in the API')",
    "The root cause section identifies a specific method, file, or query responsible — not a vague layer",
    "The agent distinguished the symptom (spinner / 500) from the root cause (the specific code or config that is broken)",
    "The report includes a 'How to confirm' section with a runnable command"
  ]
}
