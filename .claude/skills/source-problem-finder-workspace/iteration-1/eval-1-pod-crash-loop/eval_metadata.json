{
  "eval_id": 1,
  "eval_name": "pod-crash-loop",
  "prompt": "The gaku-api pod is in CrashLoopBackOff. I have no idea what's wrong. Can you find out what's causing it?",
  "assertions": [
    "The report includes a 'Layer' section identifying a specific layer (not just 'unknown')",
    "The agent ran kubectl describe or kubectl logs --previous — not just kubectl get pods",
    "The root cause section names a specific file, config key, image name, or code location (not vague)",
    "The report includes a 'How to confirm' section with a runnable command",
    "The agent gathered signals before concluding — it did not assert a cause without running diagnostic commands"
  ]
}
