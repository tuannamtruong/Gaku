{
  "eval_id": 2,
  "eval_name": "migration-failure",
  "prompt": "The db-migrate job is failing every time it runs. I added a new migration yesterday for waypoints. The Web app won't start properly either.",
  "assertions": [
    "The agent read the migrator pod logs (not just job status) to look for an error message",
    "The agent checked migration files in src/Gaku.Infrastructure/Migrations/ or ran 'dotnet ef migrations list'",
    "The root cause section identifies a specific migration file, SQL error type, or schema conflict",
    "The agent addressed both parts of the symptom: why db-migrate fails AND why the Web app won't start",
    "The report includes a 'How to confirm' section with a runnable command"
  ]
}
