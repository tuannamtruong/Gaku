apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The Job is its own kustomization so it can be applied on its own: migrations
# have to finish before the new pods start, which means a deploy applies this
# directory first and the rest afterwards. `make k8s_migrate` does that.
resources:
  - job.yaml
